# Welcome to BiteBook Frontend 👋

## Get started

1. Install dependencies

   ```bash
   npx expo install
   ```
2. Prebuild the app
   ```bash
   npx expo prebuild --clean
   ```

2. Start the app with iOS

   ```bash
    npx expo run:ios
   ```
